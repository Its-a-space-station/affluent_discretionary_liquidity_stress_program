# ADLS environment template — variable NAMES only, never commit values.
# Copy to .env (git-ignored) and fill in, or export in your shell.
# The pipeline reads this via os.getenv in src/adls/config.py only.
FRED_API_KEY=
