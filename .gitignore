# Secrets and credentials — NEVER commit these.
.env
.env.*
*.env
!.env.example
secrets/
*.key
*.pem
*.p12
*.pfx
credentials*.json
service-account*.json
*token*
*secret*
*.credentials

# Local config that may contain endpoints, account ids, or keys
config.local.*
*.local.json
.claude/settings.local.json

# Provider / broker session and cache artifacts (future)
.cache/
*.session

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
*~

# Python (in case future projects are colocated; the playbook itself has no code)
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.pytest_cache/

# Data dumps and outputs — keep research artifacts out of version control
data/
outputs/
reports/*.local.*

# As-of self-archive of no-vintage sources (approved 2026-07-19) — local only,
# never committed; the archive README documenting the routine IS committed.
data_archive/*
!data_archive/README.md
